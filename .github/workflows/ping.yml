name: Keep HuggingFace Space Alive

on:
  schedule:
    - cron: '0 */12 * * *'  # 每12小时访问一次
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Space
        run: |
          echo "Pinging Hugging Face Space..."
          curl -L -s "https://myleyu-solscan-active.hf.space" -o /dev/null
